import { WindowsHostDirective } from './windows-host.directive';

describe('WindowsHostDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowsHostDirective();
    expect(directive).toBeTruthy();
  });
});
